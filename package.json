{
  "name": "cloudflare-redirect-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for secure, configurable redirects based on subdomain.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  }
}
